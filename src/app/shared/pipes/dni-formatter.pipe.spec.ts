import { DniFormatterPipe } from './dni-formatter.pipe';

describe('DniFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new DniFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
